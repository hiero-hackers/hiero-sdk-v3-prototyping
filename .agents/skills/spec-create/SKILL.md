---
name: spec-create
description: Maintain legacy design.md and behaviors.md specifications created by the pre-Spec-Kit workflow. Use only when the user explicitly requests spec-create or works with that legacy format; use speckit-specify for new Spec Kit features.
---

# Maintain a Legacy Specification

1. Prefer `$speckit-specify` for all new feature specifications.
2. For an existing legacy specification, read `../../../.claude/skills/spec-create/SKILL.md` completely before acting.
3. Ignore references to absent `.claude/conventions` files; infer legacy structure from the existing feature artifacts.
4. Translate `/grill-me` to `$grill-me` and Claude-only tools to available Codex tools.
