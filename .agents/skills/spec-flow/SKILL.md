---
name: spec-flow
description: Complete a legacy design.md and behaviors.md workflow through implementation and review. Use only when explicitly requested for an existing legacy spec; use the speckit plan, tasks, analyze, implement, and converge skills for new work.
---

# Run a Legacy Spec Flow

1. Prefer the `$speckit-plan`, `$speckit-tasks`, `$speckit-analyze`, `$speckit-implement`, and `$speckit-converge` workflow for new work.
2. For an existing legacy specification, read `../../../.claude/skills/spec-flow/SKILL.md` completely before acting.
3. Ignore references to absent `.claude/conventions` files and translate Claude-specific commands and attribution to Codex equivalents.
4. Do not publish branches, issues, or pull requests without explicit user authorization.
