---
name: spec-implement
description: Produce an implementation plan for an existing legacy design.md and behaviors.md specification. Use only when explicitly requested for that legacy format; use speckit-plan and speckit-tasks for new Spec Kit features.
---

# Plan a Legacy Specification

1. Prefer `$speckit-plan` followed by `$speckit-tasks` for new work.
2. For an existing legacy specification, read `../../../.claude/skills/spec-implement/SKILL.md` completely before acting.
3. Ignore references to absent `.claude/conventions` files and translate Claude Code references to Codex.
4. Preserve existing project structure and repository instructions.
