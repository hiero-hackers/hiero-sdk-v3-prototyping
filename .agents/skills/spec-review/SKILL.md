---
name: spec-review
description: Review an implementation against an existing legacy design.md and behaviors.md specification. Use only for that legacy format; use speckit-analyze and speckit-converge for new Spec Kit features.
---

# Review a Legacy Specification

1. Prefer `$speckit-analyze` and `$speckit-converge` for new Spec Kit work.
2. For an existing legacy specification, read `../../../.claude/skills/spec-review/SKILL.md` completely before acting.
3. Ignore references to absent `.claude/conventions` files and translate Claude Code references to Codex.
4. Lead with concrete gaps between requirements, implementation, and tests.
